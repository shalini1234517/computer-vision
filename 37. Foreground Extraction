import cv2

img = cv2.imread('image.jpg')
gray = cv2.cvtColor(img,cv2.COLOR_BGR2GRAY)

ret,th = cv2.threshold(gray,120,255,cv2.THRESH_BINARY)

cv2.imshow("Foreground",th)
cv2.waitKey(0)
cv2.destroyAllWindows()
